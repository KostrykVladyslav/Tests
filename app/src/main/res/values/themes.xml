<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Tests" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="singInTextViewStyle">
        <item name="android:textColor">@color/green</item>
        <item name="android:textSize">50sp</item>
        <item name="android:layout_marginTop">50sp</item>
        <item name="android:layout_marginStart">50sp</item>
    </style>

    <style name="greenButtonStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="backgroundColor">@color/green</item>
        <item name="background">@color/green</item>
    </style>
    
    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@drawable/tab_background</item>
        <item name="tabIndicator">@color/white</item>
    </style>

    <style name="Theme.Tests.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="whiteTextTestName">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="toolbarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="navigationIcon">@drawable/abc_vector_test</item>
        <item name="navigationIconTint">@color/white</item>
        <item name="elevation">4dp</item>
        <item name="titleTextColor">@color/white</item>
    </style>
</resources>